version: "3.7"

services:
  builder:
    build: ./builder
    volumes:
      - ./app:/app
    command: mvn clean package
  prod:
    build: ./Prod
    ports:
      - "8080:8080"
    depends_on:
      - builder
